import requests
import os

# 1. Setup Notification Keys (We will load these securely later)
BOT_TOKEN = os.environ['TELEGRAM_TOKEN']
CHAT_ID = os.environ['TELEGRAM_CHAT_ID']

def send_notification(message):
    url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
    payload = {"chat_id": CHAT_ID, "text": message}
    requests.post(url, json=payload)

def check_slots():
    # --- YOUR ORIGINAL CHECKING LOGIC GOES HERE ---
    # Example:
    # response = requests.get("https://website.com/api/slots")
    # if "Available" in response.text:
    #     return True
    return False 

# 2. Run the logic
if check_slots():
    send_notification("🚨 SLOT AVAILABLE! Go check now!")
    print("Notification sent.")
else:
    print("No slots found.")